app:
  build: .
  ports:
    - "8080:8080"
  links:
    - db:db

db:
  image: mysql:5.7
  ports:
    - "3306:3306"
  environment:
    MYSQL_ROOT_PASSWORD: password
    MYSQL_DATABASE: tax_calculator
  volumes:
    - ./sql:/docker-entrypoint-initdb.d